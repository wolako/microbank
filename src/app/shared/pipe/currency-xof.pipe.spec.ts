import { CurrencyXofPipe } from './currency-xof.pipe';

describe('CurrencyXofPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyXofPipe();
    expect(pipe).toBeTruthy();
  });
});
